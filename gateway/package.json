{
  "name": "sam-gateway",
  "private": true,
  "version": "1.0.0",
  "description": "Optional hosted free-tier brain for SAM — pooled keys, hard cost caps. OFF by default.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260718.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.112.0"
  }
}
