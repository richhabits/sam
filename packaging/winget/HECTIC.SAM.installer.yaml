# winget installer manifest (template — {{PLACEHOLDERS}} filled by scripts/bump-packages.mjs at release).
# Submitted to microsoft/winget-pkgs under manifests/h/HECTIC/SAM/{{VERSION}}/.
PackageIdentifier: HECTIC.SAM
PackageVersion: "{{VERSION}}"
InstallerType: nullsoft
Scope: user
InstallModes:
  - interactive
  - silent
UpgradeBehavior: install
Installers:
  - Architecture: x64
    InstallerUrl: "{{INSTALLER_URL}}"
    InstallerSha256: "{{INSTALLER_SHA256}}"
ManifestType: installer
ManifestVersion: 1.6.0
